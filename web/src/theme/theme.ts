export type Theme = {
  main: {
    text: string;
    antiText: string;
    weakText: string;
    background: string;
    lightBackground: string;
    border: string;
    shadow: string;
  };
  footer: {
    text: string;
    textHover: string;
  };
};
