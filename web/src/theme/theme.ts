export type Theme = {
  main: {
    text: string;
    antiText: string;
    weakText: string;
    background: string;
    lightBackground: string;
    border: string;
  };
  footer: {
    text: string;
    textHover: string;
  };
};
